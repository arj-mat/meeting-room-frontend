import { ReceivedVoiceStage } from "../types/received-voice-stage";

export namespace RoomSecurityUtils {
    export function validateMemberVoiceStageData( received: ReceivedVoiceStage ): boolean {
        return true;
    }
}